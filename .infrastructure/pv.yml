apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-volume
  namespace: todoapp
spec:
  storageClassName: Standard
  persistentVolumeReclaimPolicy: Delete
  accessModes:
    - ReadWriteMany
  capacity:
    storage: 1Gi
  hostPath:
    path: /app/data
